---
description: Unit Test — Vitest logic tests for Portal
globs: "{composables,services,models,validations,utils,stores,tests/unit}/**/*.{ts,vue}"
alwaysApply: false
---

# /unit — Vitest

Skill: `.cursor/skills/unit/SKILL.md` · Grill: `.cursor/skills/grill-unit/SKILL.md`

- Logic only: composables, services, stores, models, validations, utils
- Mock `$apiFetch` at boundary; scoped `pnpm exec vitest run <path>`
- Extracts: `.cursor/extracts/verify-gate.md`
