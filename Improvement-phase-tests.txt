# Portal base — Improvement phase (tests)
# Context: spec → prototype đã ổn (chain pilot). Legacy không còn trong implementation lane.
# Testcase YAML = E2E only. Unit = dev tự đảm bảo, không map testcase.

================================================================================
A. E2E lane (/test, /grill-test) — DONE (2026-06-27)
================================================================================

[x] Tạo .cursor/extracts/portal-test-readiness.md
    - Gate sau /prototype, trước /test
    - Mỗi function split → testcases/{slug}-{function}.yaml (spec-split-by-function)
    - testIds.required ⊆ UI prototype
    - setup.session phải có helper trong tests/e2e/helpers/ (hoặc grill ghi #needs-session-helper)
    - 1 testcase YAML ≈ 1 tests/e2e/{module}/{function}.spec.ts
    - Không đọc legacy trong session /test

[x] Mở rộng .cursor/skills/test/SKILL.md
    - Bỏ legacy-config khỏi shared extracts (chỉ spec + testcase + code)
    - Convention: tests/e2e/pages/{module}/*Page.ts (PO, getByTestId only)
    - tests/e2e/fixtures/, helpers/session.ts registry
    - Hai mode: prototype (mock network theo testcase.setup.mocks) vs post-/wire (API thật)
    - #wire-only scenarios: chỉ mock / skip đến wire
    - Sau pass: pnpm portal:lifecycle set {route} test

[x] Mở rộng .cursor/skills/grill-test/SKILL.md
    - Matrix: requirementIds ↔ testcase ↔ spec file
    - List + pagination ≥2 pages khi spec/mock có
    - PO + getByTestId; không css/xpath
    - verify-gate: pnpm test:e2e scoped

[x] Cập nhật .cursor/skills/grill-prototype/SKILL.md
    - Handoff checklist → /test (testIds có/không, list testcase files, wire-only)
    - Bỏ “legacy-to-SPA” khỏi mô tả chính (legacy chỉ design lane opt-in)
    - Không chạy Playwright trong grill-prototype

[x] Cập nhật docs/operational/PROMPT-TEMPLATES.md (testing lane)
[x] Cập nhật docs/operational/TEAM-AI-WORKFLOW.md — thêm extract portal-test-readiness vào bảng
[x] Cập nhật .cursor/rules/team-flow-phase3-e2e.mdc — pointer portal-test-readiness

[ ] (Optional) Script pnpm test:readiness --spec … — lint testcase vs spec testIds

================================================================================
B. Unit lane (/unit, /grill-unit) — mỏng, dev-owned
================================================================================

[ ] .cursor/skills/unit/SKILL.md — ghi rõ: KHÔNG đọc testcase YAML
    - Input: spec.yaml (contract) + code (composables, services, validations)
    - Gợi ý: validations, service parser, #manual-composable logic
    - Không duplicate E2E happy path

[ ] .cursor/skills/grill-unit/SKILL.md — optional audit sau khi dev đã viết test
    - Không bắt buộc mỗi feature

[ ] KHÔNG thêm type: unit vào docs/templates/testcase.yaml

================================================================================
C. Wire + lifecycle (nối E2E)
================================================================================

[ ] .cursor/skills/wire/SKILL.md — sau wire: re-run scoped E2E; lifecycle wire
[ ] PAGE-LIFECYCLE: document test stage sau grill-test pass

================================================================================
D. Đã xong (reference — không làm lại)
================================================================================

[x] Design registry + portal-gen shell (#shell: DataListPage)
[x] Registry promotion = prototype only (không HANDOFF gen)
[x] spec → grill → prototype flow (chain pilot)
[x] DESIGN-REGISTRY-PROMOTION.md

================================================================================
E. Gợi ý thứ tự tiếp theo
================================================================================

1. (done) portal-test-readiness.md + test/grill-test skills
2. Pilot /test trên chain hotel testcase (khi sẵn sàng)
3. unit skill one-liner (no testcase) — section B
4. wire + lifecycle notes — section C
5. (Optional) test:readiness script

================================================================================
F. Out of scope portal test base
================================================================================

- Codegen Playwright từ testcase YAML
- Coverage 100% CI toàn repo
- Testcase cho unit
- Legacy trong /test / /unit sessions
