# Derived from bundle.design — grill + registry promote

id: role-domain-function

inherits: null

shell:
  tag: "#shell: DataListPage"

patterns: []
zones: []

behavior:
  create: { enabled: true, surface: page }
  update: { enabled: true, surface: page }
  delete: { enabled: true, mode: confirm_dialog }

deviations: []
