# Derived from bundle.spec — portal:gen reads this file
# contract:gen reads entities.fields — see docs/operational/CONTRACT-FIELD-REGISTRY.md

id: role-domain-function
title: Feature title
status: draft
owner: portal-team
summary: Short summary

specOrigin: requirement

codegen:
  profile: list
  entity: entity
  module: entities
  skip: []

tags: []

actors: []
entities:
  # - name: Hotel
  #   table: hotels
  #   fields:
  #     - key: id
  #       kind: scalar
  #       type: integer
  #       scopes: [response, persistence]
  #       readOnly: true
  #     - key: name
  #       kind: scalar
  #       type: string
  #       scopes: [form, response, persistence]
  #     - key: managers
  #       kind: relation
  #       cardinality: many
  #       target: User
  #       scopes: [response]
  #       persistence:
  #         type: hasMany
  #         orm: typeorm
  #       contract:
  #         read:
  #           includeOn: [list]
  #           embed: [id, full_name]

relationships: []   # derived from entities.fields — do not author duplicate graph

requirements: []

ui:
  composition:
    pattern: DataListPage
  routes: []
  filters: []
  columns: []
  testIds:
    module: entities

api:
  endpoints: []

acceptance: []
openQuestions: []
