# UNIT HANDOFF — Chain — danh sách hotel (施設一覧)

Generated from `docs/features/chain/hotel/chain-hotel-list.spec.yaml` (profile: **list**, phase: **wire**).

Prerequisite: `pnpm portal:gen` + `generated/codegen.manifest.json`.

## Test files

- _No files written._

## Skipped (already exist)

- `tests/unit/services/chain-hotel.service.wire.test.ts` — exists (use --force)

## Verify

```bash
pnpm exec vitest run tests/unit/services/chain-hotel.service.wire.test.ts
```

## Note

No new files — existing tests kept. Re-run with `--force` to overwrite.
